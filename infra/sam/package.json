{
  "name": "sfai-aws-llm-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.mjs",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.654.0",
    "@aws-sdk/client-s3": "^3.654.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.6.3"
  }
}


